import { LoginPage } from '@/components/account/account-pages';

export const metadata = {
  title: 'Login | Orchard Beach Community Group',
};

export default function Page() {
  return <LoginPage />;
}
