import { ResetPasswordPage } from '@/components/account/account-pages';

export const metadata = {
  title: 'Reset Password | Orchard Beach Community Group',
};

export const dynamic = 'force-dynamic';

export default function Page() {
  return <ResetPasswordPage />;
}
