import { SignupPage } from '@/components/account/account-pages';

export const metadata = {
  title: 'Sign Up | Orchard Beach Community Group',
};

export default function Page() {
  return <SignupPage />;
}
